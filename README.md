
``` bash

npm install schell

```

``` javascript

import { $_ } from 'schell'

const txt2md = file =>
  $_(`mv ${FILE} ${FILE.replace(/\.txt$/, '.md')}`)


$_(`npm install schell`)
  .then(() => console.log('done'))
  .catch(e => console.log(e))

```
